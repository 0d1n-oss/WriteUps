# Empty User Registration
## Register a user with an empty email and password.

Este reto consiste en crear un usuario con `email` ni `password`
Para lograr esto solo se hizo el metodo clasico para crear un usuario, con la excepcion de interceptar la peticion con burpsuite
Ya usando burpsuite solo se elimaron los datos de `email` y `password` 
